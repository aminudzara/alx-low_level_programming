#include <stdio.h>

/**
 *main - Entry point
 *@argc: command line arguments count
 *@argv: array of command line arguments
 *
 *Return: 0 on Success
 */
int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);

	return (0);
}
